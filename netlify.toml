[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA フォールバック（Functions を除く）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
